apiVersion: apps/v1
kind: Deployment
metadata:
  name: devmind-cloud
  namespace: devmind
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: devmind-cloud
  template:
    metadata:
      labels:
        app: devmind-cloud
    spec:
      containers:
        - name: devmind-cloud
          image: __REGISTRY__/devmind-cloud:test
          imagePullPolicy: Always
          env:
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: __OTEL_ENDPOINT__
          ports:
            - containerPort: 8001
          resources:
            requests:
              cpu: "250m"
              memory: "1Gi"
            limits:
              cpu: "1"
              memory: "2Gi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 6
---
apiVersion: v1
kind: Service
metadata:
  name: devmind-cloud
  namespace: devmind
spec:
  type: LoadBalancer
  selector:
    app: devmind-cloud
  ports:
    - port: 8001
      targetPort: 8001
